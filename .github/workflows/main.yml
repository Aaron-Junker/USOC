name: PhpDocumentor
on:
  push:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: aaron-junker/actions-phpDocumentor@2.0
        with:
         dir: '.'
